# Component Brief: ComponentName

Fill this in before starting a build (RULEBOOK §2). Only the fields below are needed. Anatomy, properties, variants, states, naming, and documentation are decided by RULEBOOK.md, not by this brief.

## Component name

## Intended use

## Device variants required

Yes or no. If yes, mobile is validated at 360px frame width (RULEBOOK §6).

## Supported sizes or size constraints

## Optional structural elements

Icon, avatar, dismiss control, or similar. List which are optional and their default state.

## Product-specific constraints or business rules

Do not ask the designer for a token export. `DESIGN.md` is the canonical token source (RULES §6, CLAUDE.md). Only re-verify against `tokens/` if the designer says tokens have changed.
